global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Prometheus scrapes itself for meta-monitoring.
# Application metrics are collected by Alloy via remote_write.
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
