"""{{ Name }} request/response contracts."""

from typing import Annotated

from pydantic import Field

from app.infrastructure.application import PrimaryKey, PublicEntity


class {{ Name }}CreateBody(PublicEntity):
    """Request body for creating a {{ name }}."""

    name: Annotated[
        str,
        Field(
            description="{{ Name }} name",
            min_length=1,
            max_length=255,
        ),
    ]
    # Add your fields here


class {{ Name }}UpdateBody(PublicEntity):
    """Request body for updating a {{ name }}."""

    name: Annotated[
        str | None,
        Field(
            default=None,
            description="{{ Name }} name",
            min_length=1,
            max_length=255,
        ),
    ]
    # Add your fields here


class {{ Name }}Public(PublicEntity):
    """Public {{ name }} response model."""

    id: Annotated[PrimaryKey, Field(description="{{ Name }} ID")]
    name: Annotated[str, Field(description="{{ Name }} name")]
    # Add your fields here
